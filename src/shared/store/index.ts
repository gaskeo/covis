import {
    russianInit, russiaReducer, RussiaActionType, RussiaReducer,
    worldInit, worldReducer, WorldActionTypes, WorldReducer, RegionId
} from "./models";

export type {
    RussiaReducer, WorldReducer, RegionId
};

export {
    russianInit, russiaReducer, RussiaActionType,
    worldInit, worldReducer, WorldActionTypes
};
